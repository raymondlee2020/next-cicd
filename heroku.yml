setup:
  addons:
    - plan: jawsdb-maria
      as: DATABASE
build:
  docker:
    web: Dockerfile
    migratedb: prisma/Dockerfile
release:
  image: migratedb
  command:
    - echo "DB migrations is finished."
run:
  web: npx prisma generate ; yarn start
