setup:
  addons:
    - plan: jawsdb-maria
      as: DATABASE
build:
  docker:
    web: Dockerfile
    # migratedb: prisma/Dockerfile
release:
  image: node:alpine
  command:
    - ls
    - npx prisma migrate deploy
